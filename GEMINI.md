# Project Memory: Deep Research ADK Agent

- **Goal**: Build a Python ADK agent for Google's Agent Engine using the Deep Research Interactions API.
- **Key Feature**: Persistent research context across turns using `ToolContext.state` to store `interaction_id`.
- **Status**: Planning complete. Files (`agent.py`, `requirements.txt`, `README.md`) are being created.
- **Location**: `/Users/adamfweidman/Desktop/a2a/deep_research_adk`.
- **A2A Discovery**: The agent hosts an AgentCard at `http://localhost:8001/.well-known/agent-card.json`.
- **Environment**: Managed via `.venv` (Python 3.13) using `uv`.
